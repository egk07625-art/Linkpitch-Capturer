Remove all Clerk custom design configurations

- Remove custom appearance props from ClerkProvider in app/layout.tsx
- Remove custom localization (customKoKR) from ClerkProvider
- Remove all appearance customizations from SignIn component
- Remove all appearance customizations from SignUp component
- Reset Clerk components to default styling

This initializes Clerk to use default styling and behavior, removing all custom design configurations for a clean slate.
